---
interface Link {
	label: string;
	href: string;
}

interface Props {
	links: Link[];
}

const { links } = Astro.props;
---

<nav class="quick-links" aria-label="Documentation sections">
	{links.map((link) => (
		<a href={link.href} class="quick-link">
			{link.label}
		</a>
	))}
</nav>
